# Gaps — Substrate Unification (2026-06-09)

> Logged after wiring `workflow research/deep-research` and `fetch` to `lgwks_substrate.build_run`.

## Fixed in this cycle
1. Duplicate `"do"` subparser conflict between `lgwks_workflows.py` and `lgwks_do.py` — removed the one in workflows.
2. `workflow research` and `workflow deep-research` thin `crawl_page` calls — replaced with `substrate.build_run()` producing full artifact tree (manifest.json, chunks.jsonl, vectors.jsonl, graph.db).
3. `lgwks fetch` / `lgwks crawl` shim — now routes through `build_run(max_pages=1)` instead of standalone `crawl_page()`.
4. Backward-compat helpers (`_pick_fingerprint`, `_text_from_html`, `_extract_links`) preserved in `lgwks_crawl.py` shim for existing tests.

## Remaining gaps

| # | Gap | Priority | File(s) | Route |
|---|---|---|---|---|
| 1 | **`lgwks do <intent>` URL routing** | High | `lgwks_do.py` | Natural language intent containing a URL must route to `substrate.build_run()` instead of falling through to thin legacy paths. |
| 2 | **End-to-end smoke test** | High | `tests/` | No test exercises the full `build_run()` chain against a real URL. Current tests mock `build_run()` — the Playwright render → chunk → embed → graph path is untested in CI. |
| 3 | **`lgwks_capture.py` redundancy** | Medium | `lgwks_capture.py` | Also calls `build_run()` then adds an optional "portal" layer. If that portal layer is unused, capture is redundant with substrate + `spawn.assemble_packet()`. Audit actual callers. |
| 4 | **`lgwks_pipeline.py` un-wired** | Medium | `lgwks_pipeline.py` | Registers its own `argparse` parser (`lgwks_pipeline`) but is NOT wired into the CLI via `add_parser(sub)` in `lgwks` main. Dead surface code if no entrypoint exists. |
| 5 | **`lgwks_workflows.py` thin quick-scan** | Medium | `lgwks_workflows.py` | `_do_quick_scan()` still uses `lgwks_crawl.crawl_page()` for single-page fetch. Should route to `substrate.build_run(max_pages=1)` for consistency. |
| 6 | **`--machine` propagation to substrate** | Medium | `lgwks_substrate_run.py` | Added `--json` flag to `build`/`map`, but the global `--machine` environment variable is not explicitly read by substrate commands. Verify it flows through. |
| 7 | **Delete orphan `lgwks_crawl` stealth internals** | Low | `lgwks_crawl.py` | `crawl_page` function body was already gutted and now delegates to `lgwks_browser.render`. True dead code is the original `_UA_POOL`, `_VIEWPORT_POOL`, etc. — but these are still needed by backward-compat tests. Once tests migrate to `lgwks_browser` directly, the shim can shrink further. |
