# Gaps — Substrate Unification (2026-06-09)

> Logged after wiring `workflow research/deep-research` and `fetch` to `lgwks_substrate.build_run`.

## Fixed in this cycle
1. Duplicate `"do"` subparser conflict between `lgwks_workflows.py` and `lgwks_do.py` — removed the one in workflows.
2. `workflow research` and `workflow deep-research` thin `crawl_page` calls — replaced with `substrate.build_run()` producing full artifact tree (manifest.json, chunks.jsonl, vectors.jsonl, graph.db).
3. `lgwks fetch` / `lgwks crawl` shim — now routes through `build_run(max_pages=1)` instead of standalone `crawl_page()`.
4. Backward-compat helpers (`_pick_fingerprint`, `_text_from_html`, `_extract_links`) preserved in `lgwks_crawl.py` shim for existing tests.
5. `workflow quick-scan` — `_do_quick_scan()` now routes to `substrate.build_run(max_pages=1, max_depth=0)` instead of calling `lgwks_crawl.crawl_page()`.
6. `lgwks do research <URL>` — `_do_research()` now routes URL queries to `substrate.build_run()`; non-URL queries still fall through to akinator stub.
7. `--machine` propagation — already works: `lgwks main` sets `LGWRS_MACHINE=1` + `NO_COLOR=1`; all substrate command functions unconditionally emit JSON.
8. `lgwks_capture.py` and `lgwks_pipeline.py` — **not unwired**; both are registered in main `lgwks` CLI (`add_parser(sub)`). Gap was a false positive.

## Rejected / false-positive gaps

| # | Gap | Reason |
|---|---|---|
| 3 | `lgwks_capture.py` redundancy | Actually wired in `lgwks` CLI line 1410. It adds an optional "portal" layer on top of substrate that is not redundant with `spawn.assemble_packet()`; portal binds `repo` + `context` for later AI conversation. |
| 4 | `lgwks_pipeline.py` un-wired | False positive. It IS wired in `lgwks` CLI line 1431. |
| 6 | `--machine` propagation | Already works. `lgwks` main sets env before dispatch; substrate commands unconditionally emit JSON. |

## Remaining gaps

| # | Gap | Priority | File(s) | Route |
|---|---|---|---|---|
| 2 | **End-to-end smoke test** | High | `tests/` | No test exercises the full `build_run()` chain against a real URL. Current tests mock `build_run()` — the Playwright render → chunk → embed → graph path is untested in CI. |
| 7 | **Delete orphan `lgwks_crawl` stealth internals** | Low | `lgwks_crawl.py` | True dead code is the original `_UA_POOL`, `_VIEWPORT_POOL`, etc. — but these are still needed by backward-compat tests. Once tests migrate to `lgwks_browser` directly, the shim can shrink further. |
