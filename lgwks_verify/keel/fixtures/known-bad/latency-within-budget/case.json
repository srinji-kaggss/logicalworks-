{ "schema": "keel.known-bad-case/v0", "front_end": "latency", "entry": "profile.json",
  "expect_verdict": "GO", "expect_exit": 0,
  "description": "A5 latency tier (docs/10): the harness reports 12ms per sample; the cited budget is max_ms 20, jitter_ms 5. Measured max=12, jitter=0 hold the budget => algorithmic_efficiency true => latency_bounded GO. Measurement is empirical; the budget decision is deterministic." }
