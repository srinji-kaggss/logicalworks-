# HANDOFF — 2026-06-06 · cockpit / comms / port-conformance (lgwks language layer)

Machine-first (next reader is an AI). Continues HANDOFF-2026-06-01. Session was a careful node-by-node
systems-design dialogue with the Director. Nothing here is built — it is settled *design*, with explicit
corrections to prior drafts and the open sub-nodes. Read the two SPEC corrections FIRST.

## What got committed this session
- `ADR-004-claim-hole-atom-divergence-gauge.md` — Claim+Hole one-shape atom, canonical encoding (borrow
  protobuf TLV, own canonical ordering, BLAKE3 CID = ADR-068 fact), independent L/R verifier, divergence
  gauge. **Carries a post-review CORRECTION (see D5).**
- `SPEC-port-conformance-gauge-layer.md` — the gauge layer + algebraic port to Rust/Java. **Carries a
  post-review CORRECTION at the top.**
- `SPEC-translation-chain-bytecode-semantics.md` (committed earlier, db91691) — the upstream semantics.

## The corrections (apply these; do NOT trust the uncorrected claims)
1. **Divergence gauge is P, not AI-native.** It's a generic agreement score; its only real property is
   channel-independence (B never fed by A). Demote it from the "AI-native moat" framing.
2. **Port (Rust/Java) does NOT "derive validity"** — it measures the **human-legibility residual** (how far
   we degrade to a human standard). Real validity must come from AI-native self-play physics (RLVR) — UNBUILT.
3. **Claimed-vs-observed provenance split (missing from ADR-004 D2/D4).** A `basis` edge counts only if the
   retrieval/tool-call was **harness-observed**, not LLM-asserted. Without this, L still has the LLM inside
   its own measurement. General rule: every metric = deterministic skeleton (from harness-captured emissions)
   + a minimal semantic residual routed to a FROZEN evaluator (ADR-003), never the measured model.
4. **References are desire-signals, not components.** JEPA/BERT/Bloomberg/Cities-Skylines/Rust-Java were the
   Director showing taste — extract the axiom, keep the reference as a parameter, do NOT weld the tech in.
   (I over-built JEPA into a "two-layer instrument" — that was the failure; reverted.)

## The cockpit realization (verified, zero-cost)
The cockpit/radar runs over **Claude Code itself**, no API rates, same subscription. Verified seams:
`~/.claude/settings.json` hooks (`PreToolUse/SessionStart/Stop/UserPromptSubmit` firing); full `.jsonl`
transcript per session in `~/.claude/projects/-Users-srinji/`; `claude -p --output-format=stream-json`
headless. Gauge = deterministic **fold over the transcript/hook stream** → `GaugeState` → ratatui
(`Gauge/Sparkline/Chart/Table`, v0.30). The harness writes the log, not the model → transcript-accuracy +
INV-8 channel-independence come FREE (channel B = tool_result/hook records; channel A = assistant-text).
GAP: only `PreToolUse` is wired (intent, not result); add `PostToolUse` for live channel-B completeness.
Today this is a RADAR (observe), not ENFORCEMENT (veto) — that line needs hook-deny.

## The comms node (current frontier — where to resume)
Comms = the missing node between plane (agent) ↔ radar ↔ peers. Director's model: **"eyes and ears through
echoing."** It is OVERHEAR, not address; agents are decoupled ("not working for/with each other always").
Settled shape (all ADOPT, not invent):
- **Channel topology** individual → team → global ("teams chat" desire, NOT literal Slack). Per-agent
  hash-chained log (ADR-068 Causal Tape); **echo** = metadata relays UP, cargo stays sealed (this IS ADR-079
  metadata-ping). Global chat = the union feed of echoes = the fleet's shared senses.
- **Membership** "my AIs, same channel" = a keyed set per channel; admission = signature by a member key +
  channel capability (ADR-073) + cryptographic group isolation (ADR-078). Audit = the hash-chain IS the proof
  (fixes raw ADS-B's spoofability — auditability is cryptographic, not "we logged it").
- **Frame** `{channel_id, agent_id, seq, causal_parent, payload_type, payload, capability_proof, sig}`;
  content_hash = message ID; closed versioned `payload_type` vocabulary (terminology = cut debt + optimize machine).

## OPEN sub-node — RESOLVED 2026-06-06 → ADR-005
**The echo rule** is settled: Director ratified **(a) always-echo-metadata as spine + (c) explicit-promote
for cargo**; (b) deterministic-salience DEFERRED (legal only once a non-AI salience frame-class exists —
Axiom 2). Full comms node (overhear model, topology, membership, frame, echo rule, invariants) now in
`ADR-005-comms-node-echo-channels.md`.
NEW FRONTIER (Director deferred to here): **transport posture** — hosted relay vs P2P gossip vs shared
append-log. Topology + echo model had to settle first; transport is the next decision.

## Standing axioms set this session (in memory)
- `project_non_ai_comms_substrate` — terminology = cut tech-debt + optimize machine; most comms is NON-AI
  (AI = gated exception); the echo/eyes-ears model.
- `feedback_axiom_extraction` (extended) — tech references are desire-signals, not component specs.
- `feedback_lgwks_cli_gaps` (extended) — no served CLI/TUI reference registry (`lgwks refs <topic>` wanted).

## Build sequence still standing (ADR-004 D6, unchanged)
atom + canonical encoder → independent L/R verifier → divergence gauge (mode-1 fold over THIS session's
`.jsonl` is the smallest real first gauge) → global emitter / comms. The self-play RLVR physics is the
validity keystone and remains unbuilt — until it exists, the port measures legibility, not correctness.
